/*

void updateCube(int n)                   n the number of times to run the update; gives affect of slowing down when using larger numbers.
void fade(int n)                         n how much to decrease brightness; negative number should increase brightness
void background(int r, int g, int b)     r,g,b the color to set all the bulbs to be; gives affect of clearing cube
void line(int x1, int y1, int z1, int x2, int y2, int z2, byte r, byte g, byte b)       coord1, coord2, color  creates a line of specified color between specified points
void point(int x, int y, int z, byte r, byte g, byte b)                                 coord, color           marks led at specified location the specified color
void HSL(float h, float s, float l)      hue saturation lightness input to convert function from HSL to RGB. function outputs to bytes HSLr, HSLg, HSLb
void incrementHue(float n)               n amount to increment; one more step in the hue cycle outputs new color to bytes HSLr, HSLg, HSLb




*/
